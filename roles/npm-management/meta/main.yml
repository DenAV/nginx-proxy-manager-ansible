galaxy_info:
  author: DenAV
  description: Manage Nginx Proxy Manager proxy hosts via REST API
  company: ""

  issue_tracker_url: https://github.com/DenAV/nginx-proxy-manager-ansible/issues

  license: MIT

  min_ansible_version: "2.12"

  platforms:
    - name: Ubuntu
      versions:
        - focal
        - jammy
        - noble
    - name: Debian
      versions:
        - bullseye
        - bookworm
    - name: EL
      versions:
        - "8"
        - "9"

  galaxy_tags:
    - nginx
    - proxy
    - reverseproxy
    - docker
    - letsencrypt
    - ssl
    - certificates
    - api

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
